{{#if user}}

  <ul class="nav navbar-nav navbar-right">

    <li class="dropdown">


      <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <span class="glyphicon glyphicon-user"></span> 
        <img src="{{user.avatar_url}}" width="30" height="30" class="d-inline-block align-middle" alt="">
        <span class="navbar-text">{{user.name}}</span>
        <span class="glyphicon glyphicon-chevron-down"></span>
      </a>

      <ul class="dropdown-menu">
        <li>
          <div class="navbar-login p-2">
            <div>
              <p class="text-left"><strong>{{user.name}}</strong></p>
              <a href="/auth/full_access" class="btn btn-primary btn-block btn-sm">Full access</a>
              <a href="/auth/logout" class="btn btn-secondary btn-block btn-sm">Logout</a>
            </div>
          </div>

        </li>
      </ul>


    </li>

  </ul>

{{/if}}